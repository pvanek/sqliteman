This directory contains documentation for Sqliteman.

Docs are written in KDE based Docbook. Documentaion building is
cmake-based too!


Directories:

    common
    - KDE4 common graphics and styles for HTML files

    docbook
    - local copy of the Docbook source

    en
    - Endlish documentation


simple usage. E.g.:
1) create a "build" directory: mkdir build
2) cd mkdir
3) cmake -DCMAKE_INSTALL_PREFIX=../release ..
4) make install
5) see the "release" directory for new documentation



